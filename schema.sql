CREATE TABLE destinations(
    id SERIAL PRIMARY KEY,
    destination TEXT,
    price TEXT
    duration TEXT,
);


CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    username TEXT,
    password TEXT
);

